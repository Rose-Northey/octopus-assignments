import { warmupExercise } from "./warmup-exercise";

describe("Test for myPromise", () => {
    // test("myPromise resolves correctly", async () => {
    // //     const result = await myPromise;
    // //     console.log(result);
    // //     expect(result).toBe("Program complete");
    // // });

    test("", async () => {
        const result = await warmupExercise();
        expect(result).toBe("Program complete");
    });
});
